# LangChain-RAG-System
A basic implementation of Retrieval-Augmented Generation (RAG) using LangChain, allowing users to query a custom document (PDF) by combining vector search with a Large Language Model (LLM).
